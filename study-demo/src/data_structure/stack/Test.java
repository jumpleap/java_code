package data_structure.stack;

public class Test {
    public static void main(String[] args) {
        Node head = Node.createNode();
        LeetCode code = new LeetCode();
        code.printReverse1(head);
        code.printReverse(head);
    }
}
